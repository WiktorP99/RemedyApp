<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.remedy">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.ar" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/remedy_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".CoolingFluidScanning"
            android:exported="true" />
        <activity
            android:name=".WindshieldWasherFluidActivity"
            android:exported="true" />
        <activity
            android:name=".BrakeFluidActivity"
            android:exported="true" />
        <activity
            android:name=".OilRefillActivity"
            android:exported="true" />
        <activity
            android:name=".OilLevelActivity"
            android:exported="true" />
        <activity
            android:name=".CoolingFluidActivity"
            android:exported="true" />
        <activity android:name=".ArCoreActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".AppInfoActivity" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".CarScanActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!--  -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <meta-data android:name="com.google.ar.core" android:value="required" />
    </application>

</manifest>